test 


<img>
  {{#each pictures}}
    <h3>{{ this.name }}</h3>
    
    <img src="{{ this.path }}" alt="{{ this.originalName }}" width="200px"></img>
    <a href="{{ this.path }}" download>download</a>
  {{/each}}
</section>

